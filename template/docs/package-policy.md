# Package Policy

Pinned versions only. The committed `eng/local-feed` analyzer package and `src/Product.Api/packages.lock.json` are a matched pair and must be refreshed together. Package versions are immutable; bump the analyzer version whenever package content changes.
